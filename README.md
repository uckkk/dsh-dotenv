# dsh-dotenv · .env 解析与序列化

纯 Node 实现，无网络、无外部服务。

## 安装

```bash
dsh plugin add dsh-dotenv
```
安装后在 profile 的 `package.json` 的 `dsh.profile.bundles` 中加入 `"dsh-dotenv"`。

## 安装

```bash
dsh plugin add github:uckkk/dsh-dotenv
```

## 使用

安装后在会话中调用该插件注册的工具即可。
